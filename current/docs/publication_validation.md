# 发布整理与验证记录

检查日期：2026-09-20。运行环境：Linux、MATLAB R2025b。

## 整理范围

- `current/` 来自原 ScopeGuide 工作目录，包含 RCM 三自由度、Cartesian 六自由度、共享模块、标定与诊断代码、技术文档和离线测试。
- 两套主入口及早期 dual-pivot 配置使用的基础 MAT 配置已放入 `resources/`。默认加载路径、进程工作目录、标定路径和结果目录按发布目录解析。
- 控制算法、调参值、运动授权门和保护阈值保留工作版本；RCM 启动时覆盖确认短语的原有步骤保留在启动说明中。
- `archives/scopeguide_3dof_admittance_drag_final_20260821/` 包含原归档文件，包括其尚未提交的源码修改，不包含嵌套 `.git`。原清单另存为 `MANIFEST_SHA256.original.txt`，更新后的清单覆盖当前归档文件。
- 根目录初始版入口及依赖保留，初始版 README 存于 `docs/initial_release_readme.md`。
- 原工作目录与原归档均未删除或改写。

## 检查结果

| 检查 | 结果 |
|---|---|
| MATLAB 离线回归测试 | 193 / 193 通过；0 失败，0 跳过 |
| 两套入口和参数入口的静态依赖分析 | 71 个代码文件；无其他本地项目目录依赖 |
| RCM 与 Cartesian 配置对比 | 除资源及结果路径外，与整理前工作版本逐字段一致 |
| 归档源码与资源 | 与原归档逐字节核对；仅重新生成校验清单 |
| 原始文件保留 | 复制清单中的 274 个源文件通过 SHA-256 复核 |

测试资源包含两份多姿态标定 CSV、两份力传感器 CSV、标定 JSON 和两份基础 MAT 配置。完整实验结果和其余历史数据未随仓库发布。

整理期间修正了过时的测试假设：六自由度稳态速度测试按配置的设计力输入；RCM 参数快照断言对应当前工作版本的时间常数、插入范围和设计力；基础配置测试改为读取随仓库提供的资源。新增测试检查默认资源解析及外部标定路径保留。

以上检查均为离线验证，没有连接机械臂或传感器，没有发送运动命令。未重新进行实机拖拽或实时控制时序验收；历史实验配置和记录不构成对其他硬件安装的验证。
